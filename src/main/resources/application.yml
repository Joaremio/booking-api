spring:
  application:
    name: booking
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/booking}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1234567}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
jwt:
  secret: ${JWT_SECRET:xWlGipCAQcZVaD7WtCxEAPKM554DF1N3taZfh+sboJTaHW+JRC3H5B7iJxJB+znjJFm+YJs6jywSx5sxGbik5g==}
  expiration: "86400000"

app:
  admin:
    email: ${ADMIN_EMAIL:admin@email.com}
    password: ${ADMIN_PASSWORD:admin123456}